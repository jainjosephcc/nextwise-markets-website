{
  "name": "nextwise-framer-local",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check app.js && node --check policy.js && node --check server.mjs",
    "preview": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
